#!/usr/bin/env bash

CHOICES=( \
	'( ͡° ͜ʖ ͡°)' \
	'¯\_(ツ)_/¯' \
	'🌚' \
	'🅱️' \
	'☭' \
	'Σ' \
)

CHOICE="$(printf '%s\n' "${CHOICES[@]}" | rofi -dmenu -format i)"

if [ ! -z "$CHOICE" ]; then
    echo -n "${CHOICES[$CHOICE]}" | xclip -sel clip
fi

